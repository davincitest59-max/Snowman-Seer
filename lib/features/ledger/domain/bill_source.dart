enum BillSource { wechat, alipay, unknown }

enum TransactionDirection { income, expense, unknown }

enum RecordOrigin { notification, fileImport, manual }

enum ConfirmationStatus { confirmed, pending }
